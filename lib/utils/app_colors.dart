import 'package:flutter/material.dart';

class AppColors {
  // Primary & Brand
  static const Color primary = Color(0xFF508CAD);
  static const Color secondary = Color(0xFF9ECDE7);
  static const Color accent = Color(0xFF7BAAC3);

  // Surface & Background Colors
  static const Color surfacePrimary = Color(0xFFFFFFFF);
  static const Color surfaceSecondary = Color(0xFFF9FAFB);
  static const Color surfaceAccent = Color(0xFFE0EFF7);
  static const Color surfaceCool = Color(0xFFF0F3F8);

  // Text & Neutral Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFFA8A8A8);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Border & Divider
  static const Color border = Color(0xFFE9E9E9);

  // Status / Semantic Colors
  static const Color error = Color(0xFFD92D20);
  static const Color success = Color(0xFF118611);
}
